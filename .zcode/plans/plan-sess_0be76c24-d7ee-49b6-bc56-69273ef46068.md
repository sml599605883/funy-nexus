将上传页弹窗改为 Mine `Account` 点击后的第一层绿色操作面板，而不是红橙色的退出挽留确认框或当前白色底部 sheet。

1. 在 Mine widgets 中抽取只负责视觉框架的通用双操作面板：保留现有 375 x 812 设计坐标、绿色顶部胶囊、浅绿渐变主体、两个白色操作按钮和底部关闭图片按钮。
2. 保持 `AccountPanelDialog` 对 Mine 的外部接口、`Log out / Delete Account` 文案、返回 `AccountExitAction` 的行为不变，让 Mine 页面与其已有测试继续使用该包装组件。
3. 新建上传专用包装弹窗，复用同一个绿色面板框架，将两个操作替换为 `Camera` 和 `Album`，并返回现有 `IdentityUploadMethod.camera / album`；关闭按钮、点击遮罩和返回键直接关闭弹窗。
4. 修改 `IdentityUploadPage`：底部 `Upload` 打开新的居中绿色面板；拿到 Camera/Album 选择结果后，继续执行当前已实现的权限请求、选图、压缩、OCR 上传、身份确认和产品续办流程。不会改动这些后续业务逻辑。
5. 删除不再使用的白色底部 Camera/Album/Quit sheet 与其颜色 token，避免保留两种上传弹窗实现。
6. 更新上传页 widget 测试，验证 Upload 打开居中绿色面板、Camera/Album 文案和关闭动作；保留 Mine 的回归测试。最后执行 `dart format`、`flutter analyze`、`flutter test`。